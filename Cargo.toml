[package]
name = "saldo"
version = "0.1.0"
edition = "2024"

[dependencies]
ariadne = "0.6.0"
chrono = { version = "0.4", default-features = false, features = ["alloc"] }
indexmap = "2"
rust_decimal = { version = "1.41.0", default-features = false }
